spring:
  config:
    activate.on-profile: local
  cloud:
    config:
      uri: ${CONFIG_STORE_URL:http://localhost:8999}
      fail-fast: true
---
spring:
  config:
    activate.on-profile: stage
  cloud:
    config:
      uri: ${CONFIG_STORE_URL:http://host.docker.internal:8999}
      fail-fast: true